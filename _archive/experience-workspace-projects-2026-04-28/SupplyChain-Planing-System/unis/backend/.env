DB_HOST=localhost
DB_PORT=5432
DB_NAME=unis_scp
DB_USER=postgres
DB_PASSWORD=postgres

# CORS — danh sách frontend URL cho phép (comma-separated)
# Production: đổi thành URL thực của frontend (https://your-app.vercel.app)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Port backend (mặc định 3002 nếu không set)
PORT=3002
